{
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "2.7.1",
    "prettier-eslint": "^15.0.1"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "cd client && npx prettier --read ",
    "format": "cd client && npx prettier --write .",
    "scriptname": "cmd"
  }
}
